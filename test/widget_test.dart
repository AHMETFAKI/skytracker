// Placeholder smoke test. Real unit/widget tests are added in Phase 8
// (unit_converters, validators, failure_mapper, DTO mapping).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — build pipeline is green', () {
    expect(1 + 1, 2);
  });
}
